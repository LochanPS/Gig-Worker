Contract ABIs land here. P1 (track/chain) commits:
  MockUSDC.json  IdentityRegistry.json  EscrowVault.json  AuditAnchor.json
and the deploy script writes addresses.local.json (gitignored) at boot.
Until then this directory is a placeholder so the workspace resolves.

================================================================================
COPY-PASTE PROMPTS FOR THE THREE BUILD SESSIONS (Opus)
One prompt per cloud account. Paste verbatim as the first message.
Start P2 FIRST; start P1 and P3 immediately after (they have standalone
work until P2's scaffold merges).
================================================================================

--------------------------------------------------------------------------------
PROMPT FOR SESSION P2 — BACKEND & AGENT (start this one first)
--------------------------------------------------------------------------------
You are one of three parallel Claude Code sessions building "GigBridge", an
autonomous cross-border freelancer payment gateway, in the repository
LochanPS/Gig-Worker. Your track is P2 — BACKEND & AGENT. Work on branch
track/backend and push continuously. The other two sessions are P1 (smart
contracts + settlement, branch track/chain) and P3 (frontend, branch
track/frontend).

FIRST: read these files on branch claude/gig-worker-cross-border-rmy2jo (or
main if already merged): PRD.txt, TRD.txt, ROADMAP_3_PERSON_3_DAY.txt,
BUILD_CONTRACTS.txt, UI_SPEC.txt. BUILD_CONTRACTS.txt is the frozen
integration contract — the API paths, enums, ports, env vars, seed data and
thresholds there are binding. If you must change anything in it, append a
dated note to INTEGRATION_LOG.txt in the same commit.

YOUR FIRST DELIVERABLE (do this before anything else, ~90 min): scaffold the
pnpm monorepo exactly as TRD 1.2 describes (contracts/, backend/, frontend/,
shared/, infra/, docs/ — move the .txt docs into docs/), create the /shared
package with ALL types, enums and zod schemas from BUILD_CONTRACTS.txt
sections 3-5, docker-compose skeleton (postgres + anvil + backend +
frontend), .env.example per section 2, and a trivial mock API server
(backend/mock/) that serves every endpoint in section 4 with seed-shaped
fake data so P3 can build against it. Merge this to main immediately and
append "SCAFFOLD MERGED <timestamp>" to INTEGRATION_LOG.txt.

THEN build, in order (details in TRD sections 4 and ROADMAP Day 1-3 / P2
rows): (1) Prisma schema + migrations for the full data model (TRD 4.1);
(2) auth: register/login/JWT/RBAC; (3) payment orchestrator implementing
the state machine in TRD 4.2 with audit logging and websocket broadcast of
every transition; (4) FX service: frankfurter.app live rates + committed
offline fallback JSON, quote endpoint with 75bps fee and 10-min rate lock;
(5) compliance rule engine: all 10 rules in TRD 4.5 with the exact
thresholds in BUILD_CONTRACTS.txt section 8, decision persistence, admin
queue endpoints; (6) agent service: Anthropic API (claude-sonnet-5) to
generate plain-English reasoning traces from rule results, with a
template-based fallback when ANTHROPIC_API_KEY is empty — the LLM explains,
deterministic rules decide; (7) anomaly detection (velocity, structuring,
outlier) writing Alert rows; (8) seed script implementing BUILD_CONTRACTS
section 7 exactly, plus a demo:reset script; (9) invoice endpoints;
(10) PDF receipt/compliance-report endpoints (lowest priority).

INTEGRATION: P1 will commit contract ABIs to /shared/abis and a
contracts/deploy.ts module — integrate their settlement wrappers into your
orchestrator on Day 2 (the fund/release calls happen at the FUNDED/SETTLING
transitions). Until then, stub the settlement calls behind an interface.
Pull main before every push; merge track/backend to main at every roadmap
sync point. Write tests for the state machine and every compliance rule.
Never commit secrets. Keep commits small with clear messages.

--------------------------------------------------------------------------------
PROMPT FOR SESSION P1 — CHAIN & SETTLEMENT
--------------------------------------------------------------------------------
You are one of three parallel Claude Code sessions building "GigBridge", an
autonomous cross-border freelancer payment gateway, in the repository
LochanPS/Gig-Worker. Your track is P1 — CHAIN & SETTLEMENT. Work on branch
track/chain and push continuously. The other sessions are P2 (backend,
branch track/backend — it scaffolds the monorepo first) and P3 (frontend).

FIRST: read PRD.txt, TRD.txt (especially section 3), ROADMAP_3_PERSON_3_DAY
.txt and BUILD_CONTRACTS.txt (especially section 6 — the contract function
signatures and events there are FROZEN; the backend and frontend are built
against them). Changes require a note in INTEGRATION_LOG.txt.

START IMMEDIATELY as a standalone Foundry project in /contracts (do not
wait for the monorepo scaffold; once INTEGRATION_LOG.txt says "SCAFFOLD
MERGED", rebase onto main and fit into the workspace). Build, in order:
(1) MockUSDC.sol — ERC20, 6 decimals, onlyOwner mint; (2) IdentityRegistry
.sol — credential hash per address, expiry, revocation, isVerified view;
(3) EscrowVault.sol — the core: Payment struct, states None/Funded/
Released/Refunded/Frozen, fund/release/refund/freeze/unfreeze with the
exact signatures in BUILD_CONTRACTS section 6, OpenZeppelin AccessControl
(PLATFORM_ROLE) + ReentrancyGuard, verified-party gate via
IdentityRegistry, fee split to treasury, all four events; (4) AuditAnchor
.sol. Write a thorough forge test suite: every state transition, every
access-control failure, reentrancy, double-release, refund-after-release
must revert. All tests green before anything else.

THEN: (5) commit ABIs to /shared/abis/*.json; (6) write contracts/
deploy.ts — a viem-based TypeScript module the backend imports to deploy
all four contracts to anvil on boot when absent, writing addresses to
/shared/abis/addresses.local.json; (7) build the settlement service inside
backend/src/settlement/: viem wallet clients (env keys per BUILD_CONTRACTS
section 2), typed wrappers fundEscrow/releaseEscrow/refundEscrow/
setCredential/anchorDecision, an event listener that watches
PaymentFunded/Released/Refunded and invokes orchestrator callbacks, and
demo wallet generation + MockUSDC faucet minting; (8) on Day 2 afternoon,
pair with P2 (coordinate via INTEGRATION_LOG.txt and small PRs to main) to
wire the full pipeline: confirm -> fund on-chain -> event -> SETTLING ->
release -> COMPLETED; (9) Day 3 nice-to-have only after everything works:
deploy to Polygon Amoy and add explorer links; (10) failure drill: verify
everything works with zero internet (anvil only).

Pull main before every push; merge to main at every roadmap sync point.
Never commit real private keys — anvil's well-known dev keys only, marked
DEMO ONLY.

--------------------------------------------------------------------------------
PROMPT FOR SESSION P3 — FRONTEND & DEMO
--------------------------------------------------------------------------------
You are one of three parallel Claude Code sessions building "GigBridge", an
autonomous cross-border freelancer payment gateway, in the repository
LochanPS/Gig-Worker. Your track is P3 — FRONTEND & DEMO. Work on branch
track/frontend and push continuously. The other sessions are P2 (backend —
scaffolds the monorepo and a mock API server first) and P1 (contracts).

FIRST: read PRD.txt, UI_SPEC.txt (your primary spec — implement it
faithfully), DEMO_SCRIPT.txt (what you build must make this script
performable), ROADMAP_3_PERSON_3_DAY.txt, and BUILD_CONTRACTS.txt
(sections 3-5 and 7: the API contract, websocket events and seed users you
will render; these are frozen).

START IMMEDIATELY in an isolated Vite app if the monorepo scaffold has not
merged yet (INTEGRATION_LOG.txt will say "SCAFFOLD MERGED"); move into
frontend/ once it exists. Stack: React 18 + Vite + TypeScript + Tailwind +
shadcn/ui + Recharts. Build against P2's mock API server first, real API
from Day 2. Design language per UI_SPEC section 1 (dark navy fintech, the
exact status-chip colors, mono for money/hashes).

BUILD ORDER: (1) auth pages + role-routed shell (company/freelancer/admin
layouts, left nav + topbar); (2) shared components: StatusChip, QuoteCard,
FeeBreakdown, PaymentTimeline, AgentReasoningPanel (typewriter effect),
TxHashLink, CredentialCard; (3) Company overview + payouts table +
onboarding steppers; (4) THE CENTERPIECE — the 4-step New Payout wizard
per UI_SPEC 3.2 including live quote, "vs PayPal" comparison strip,
live-ticking compliance checklist, agent reasoning stream, rate-lock
countdown ring; (5) websocket client + live payment timeline (company
detail page and freelancer incoming view must tick in real time);
(6) freelancer dashboard per UI_SPEC 4 including the "You kept 99.X%"
banner and confetti on completion; (7) admin dashboard per UI_SPEC 5:
live monitor feed, compliance queue with approve/reject + notes, alerts
page with pattern evidence, rules registry, treasury; (8) invoices both
sides + batch payout; (9) notifications (toasts + bell menu); (10) polish:
FX history charts, empty/loading states, demo choreography for three tiled
windows.

DAY 3 YOU ALSO OWN: the pitch deck (structure in DEMO_SCRIPT minute 5-6
and PRD section 8) and a backup demo recording. Rehearse DEMO_SCRIPT
end-to-end; anything in the script that feels clunky in the UI, fix.

Pull main before every push; merge track/frontend to main at every roadmap
sync point; log any /shared change needs in INTEGRATION_LOG.txt for P2.
================================================================================

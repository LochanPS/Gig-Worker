INTEGRATION LOG — append-only. Any change to BUILD_CONTRACTS.txt or /shared must be noted here in the same commit.
==============================================================
[2026-09-03] SCAFFOLD MERGED (P2 / track/backend)
  - pnpm monorepo: shared / backend / frontend / contracts / infra / docs.
  - /shared package published as @gigbridge/shared with ALL enums, constants
    (frozen thresholds), entity types, zod request schemas, and WS event
    contract from BUILD_CONTRACTS sections 3-5 & 8.
  - Mock API server at backend/mock (serves every endpoint in section 4 with
    seed-shaped fixtures) so P3 can build against http://localhost:4000 now.
  - docker-compose skeleton (postgres + anvil), .env.example per section 2.
  - Docs moved into /docs. No BUILD_CONTRACTS values changed.
  P1: rebase onto main, put Foundry work in /contracts, commit ABIs to
      /shared/abis. P3: rebase onto main, put Vite app in /frontend.

[2026-09-03] Backend step 7 + CONTRACTS_AND_SETTLEMENT doc (P2 / track/backend)
  - Anomaly alerts persisted (Alert rows) + live alert.new WS events to admins;
    /admin/alerts + /admin/alerts/:id/resolve endpoints. No contract changes.
  - Added docs/CONTRACTS_AND_SETTLEMENT.txt: the settlement port + contract
    handoff guide for P1. Backend side of the seam is done via simulatedSettlement;
    P1 implements the same Settlement interface for real and calls setSettlement().

[2026-09-03] Backend step 8: seed + demo:reset + init migration (P2 / track/backend)
  - seed.ts creates the 7 demo actors, credentials, ~43 historical payments, and
    the structuring pattern (on Novatek->Alex so scenario 1 stays a clean APPROVE).
  - reset.ts (demo:reset) wipes FK-safe and reseeds; verified idempotent.
  - Prisma init migration committed (20260903104300_init).
  - FIX: orchestrator transitioned DRAFT->FLAGGED/REJECTED directly (illegal);
    now goes DRAFT->COMPLIANCE_CHECK then branches. Found via real e2e run.
  - PROVEN end-to-end on real Postgres: login/quote/create/compliance/confirm/
    settle/COMPLETED, admin queue, live alerts. Updated CONTRACTS_AND_SETTLEMENT §7-8.
[2026-09-03] ABIS COMMITTED (P1 / track/chain)
  - Foundry contracts done + fully forge-tested (34 tests): MockUSDC,
    IdentityRegistry, EscrowVault, AuditAnchor. Signatures per BUILD_CONTRACTS
    section 6 (frozen).
  - ABIs exported to shared/abis/{MockUSDC,IdentityRegistry,EscrowVault,
    AuditAnchor}.{json,ts} + index.ts. The .ts files export each ABI `as const`
    for viem typed calls. Regenerate via `node contracts/script/export-abis.mjs`.
  - Deps vendored into contracts/lib (OZ v5.1.0 + forge-std) so builds are
    offline-safe (NFR-4). solc 0.8.24.
  - NOTE for P2 (settlement seam): on-chain EscrowVault.fund takes
    `bytes32 id`. Your settlement.interface.ts fund() receives paymentId (uuid
    string); the real wrapper will compute id = keccak256(toUtf8(paymentId))
    and RETURN it as escrowId. Backend already stores escrowId — keep passing
    the uuid; the wrapper owns the bytes32 derivation. Confirmed compatible
    with orchestrator confirmPayment().
  - STILL TODO (P1): contracts/deploy.ts (viem deploy-on-boot ->
    shared/abis/addresses.local.json), real settlement service in
    backend/src/settlement, event listener. Lands Day 2.

[2026-09-03] Backend step 9: invoices (P2 / track/backend)
  - Freelancer raises invoice -> company inbox -> approve creates a payment via the
    SAME orchestrator (compliance/quote/settle). invoiceRef links them; invoice
    auto-marked PAID on payment COMPLETED. Notifications on received/approved.
  - /invoices (POST freelancer, GET both), /invoices/:id/approve (company).
  - Added CreateInvoiceInput export to shared. No contract/settlement changes;
    updated CONTRACTS_AND_SETTLEMENT §8. Proven end-to-end on real Postgres.

[2026-09-03] Backend step 10: documents — BACKEND COMPLETE (P2 / track/backend)
  - receipt.pdf + compliance.pdf as print-ready HTML (browser print-to-PDF,
    no heavy PDF dep). Receipt: parties/amounts/FX/fee/net + on-chain tx hashes.
    Report: verdict, all rule results w/ legal refs, agent reasoning, anchor hash.
  - HTML-escaped (injection-safe), auth-gated (401 without token). Proven live.
  - All 10 backend steps done; 34 tests green. Settlement seam (§3) is the only
    remaining P1 integration point. Updated CONTRACTS_AND_SETTLEMENT §9.

[2026-09-03] DEPLOY-ON-BOOT ready (P1 / track/chain)
  - contracts/deploy.ts (viem): ensureDeployed() deploys the four contracts to
    anvil when absent, writes shared/abis/addresses.local.json (gitignored),
    idempotent. Verified against live anvil (all four deploy, 2nd run no-op,
    on-chain code confirmed). Backend bootstrap: `import { ensureDeployed } from
    "@gigbridge/contracts"; await ensureDeployed();`.
  - shared/abis/*.json now carry { abi, bytecode } (deploy needs no Foundry).
    Regenerate: `node contracts/script/export-abis.mjs`.
  - contracts/ is now a workspace package @gigbridge/contracts (viem + tsx).
    ROOT CHANGE: pnpm-workspace.yaml gained "contracts" — run `pnpm install`.
  - Added docs/ROADMAP_SIMPLE.txt; updated CONTRACTS_AND_SETTLEMENT §6/§9.
  - NEXT (P1): backend real-settlement.ts implementing the Settlement port.

[2026-09-03] Frontend scaffold (P2 acting on track/frontend — P3 had not started)
  - React 18 + Vite + TS + react-router, design tokens per UI_SPEC §1.
  - Typed api client (@gigbridge/shared), AuthProvider, useWs hook.
  - Pages: Login, Company Overview + New Payout WIZARD (quote->compliance->settle),
    Freelancer Home, Admin Monitor (queue + alerts). All against the REAL API.
  - VERIFIED: frontend build passes; login through the Vite proxy hits the live
    backend and returns the seeded user. Core demo path clickable.
  - Added docs/FRONTEND_INTEGRATION.txt (the relevant doc for this path).
    CONTRACTS_AND_SETTLEMENT.txt (P1-owned) is untouched — frontend doesn't touch it.
  NOTE for P3: pick this up on track/frontend; §4 of FRONTEND_INTEGRATION lists
  the remaining screens + polish.

[2026-09-03] REAL SETTLEMENT implemented (P1 / track/chain)
  - contracts/chain.ts: createChainOps() — low-level viem ops (fund/release/
    refund/anchor/setCredential/mint/sendGas/reads); ABIs from committed JSON.
  - backend/src/modules/settlement/real-settlement.ts: implements the Settlement
    port for real (resolves payer demo key from DB by paymentId; escrowId =
    keccak256(uuid); minor->USDC x10^4). ensureChainReady() boot step deploys +
    gas-tops-up + setCredential + faucet-mints verified actors. enableRealSettlement()
    wired into backend/src/index.ts, GATED by SETTLEMENT_MODE=real; falls back to
    simulatedSettlement on any chain error (risk R1). Default behaviour unchanged.
  - ROOT/BACKEND CHANGE: backend/package.json gained deps viem + @gigbridge/
    contracts (workspace); contracts/package.json exports "./chain". Run
    `pnpm install`. To demo real chain: anvil up + SETTLEMENT_MODE=real.
  - PROVEN on live anvil (contracts/script/itest-settlement.ts): payer funds
    escrow -> platform releases -> payee USDC = amount-fee exactly. Backend
    typecheck clean, 34 backend tests green, 34 forge tests green.
  - Updated CONTRACTS_AND_SETTLEMENT §6/§10. NEXT: Day-2 full-stack e2e with
    Postgres + SETTLEMENT_MODE=real; optional event listener; Day-3 Amoy.

[2026-09-03] FULL-STACK REAL-CHAIN E2E PASSED (P1 / track/chain) — DAY-2 CHECKPOINT
  - Stood up Postgres (embedded-postgres, no Docker needed) + anvil, migrated +
    seeded, booted backend with SETTLEMENT_MODE=real, and drove the real REST API:
    login -> POST /payments -> confirm -> COMPLETED with REAL fund/release tx
    hashes; on-chain escrow Released; payee MockUSDC = amount-fee (EUR 500 ->
    496.25 USDC). Harness: contracts/script/e2e-real-chain.ts.
  - BUGFIX (in real-settlement, no P2 files touched): the seed stores
    User.walletKey as random bytes UNRELATED to User.walletAddress. Real
    settlement now derives every on-chain address from walletKey (payer signer,
    payee, credential, mint, gas). walletAddress is display-only.
  - FLAG for P2 (optional, non-blocking): for a fully faithful demo seed
    walletAddress = privateKeyToAccount(walletKey).address. Settlement works
    without this.
  - Core P1 settlement path COMPLETE + proven. Remaining are nice-to-haves:
    event listener (fire-and-confirm) + Day-3 Polygon Amoy.

[2026-09-03] EVENT LISTENER done (P1 / track/chain)
  - chain.ts watchEscrow(): polls EscrowVault events; wired into
    enableRealSettlement() — backend logs every PaymentFunded/Released/Refunded
    live with tx hash + block (on-chain source of truth for the live feed,
    PRD FR-5.3). Verified firing during the e2e.
  - e2e-real-chain.ts hardened: per-escrow-record assertions (history-
    independent) + auto-approves via /admin/queue/:id/resolve when the anomaly
    rules FLAG a repeated demo payment — so it also exercises the admin path.
  - P1 TRACK COMPLETE: contracts + settlement + event listener, proven full-
    stack. Only deferred item is Day-3 Polygon Amoy (venue-risk; local anvil is
    the demo path). Backend typecheck clean; 34 backend + 34 forge tests green.

[2026-09-04] PRODUCT BUILD-OUT: batch pay-run + recurring payouts + verification (P5)
  SHARED CONTRACT CHANGED (logged per the coordination rule):
  - enums.ts: added PAYRUN_STATUSES/PayRunStatus, CADENCES/Cadence, CADENCE_DAYS.
  - schemas.ts: added payRunItemSchema, createPayRunSchema, createScheduleSchema
    (kycSubmitSchema/kybSubmitSchema already existed, now consumed).
  - types.ts: added PayRun, PayoutSchedule, VerificationResult entity types.
    NO existing enum/schema/type values changed — additive only. shared rebuilt.
  PRISMA: new models PayRun, PayoutSchedule + enums PayRunStatus, Cadence; Payment
    gained payRunId/scheduleId (nullable FKs, SET NULL) + indexes. Migration
    20260904120000_payrun_schedule (hand-written to match init style; prisma
    validate + generate clean). No changes to existing columns.
  BACKEND (all reuse the existing orchestrator — NO new settlement machinery):
  - modules/verification: self-serve KYC (freelancer) / KYB (company) → provisions
    a demo wallet + issues an on-chain-anchored credential via the settlement seam
    + flips profile VERIFIED, so new signups pass EscrowVault's verified gate.
    Routes: GET /verification/me, POST /verification/kyc, POST /verification/kyb.
    (Closes FR-1; the real KYC vendor drops in behind submitKyc/Kyb unchanged.)
  - modules/payrun (FR-2.5): createPayRun fans out into N child payments (one
    compliance eval each), confirmPayRun confirms only APPROVED children. Routes:
    POST /payruns, POST /payruns/:id/confirm, GET /payruns[/:id].
  - modules/schedules: recurring retainers. runDueSchedules() creates + auto-
    confirms a payment per due schedule via the orchestrator, advances nextRunAt.
    Opt-in background tick (env SCHEDULES_ENABLED, default off) + manual POST
    /schedules/run-due. Routes: POST /schedules, GET /schedules, pause/resume.
  - app.ts registers the three modules; index.ts wires the optional tick.
  FRONTEND: api.ts client methods; company pages Batch pay + Recurring; Verify
    page (both roles); Login now has a sign-up toggle (self-serve). Nav updated.
  VERIFIED: shared build clean; backend typecheck + 45 tests green (was 34, +11
    schema tests); frontend typecheck + vite production build clean. Migration is
    additive and not yet applied to any live DB (run prisma migrate deploy on P4).

[2026-09-04] UNHAPPY PATHS: KYC-fail + payout accounts + disputes/reversals
  Motivation: the product-scope gap analysis (branch claude/product-scope-roadmap;
  note: it read the DOCS-ONLY default branch and wrongly concluded "no code" — the
  real product is on main). Its #3 (unhappy-path screens real regulated apps must
  show) is the genuine gap; this builds it. All additive to the proven happy path.
  SHARED CONTRACT CHANGED (logged):
  - enums.ts: PAYMENT_STATES += PAYOUT_FAILED, DISPUTED, REVERSED (additive; happy
    path DRAFT..COMPLETED unchanged). Added DISPUTE_STATUSES/DisputeStatus.
  - schemas.ts: addPayoutAccountSchema, raiseDisputeSchema, resolveDisputeSchema.
  - types.ts: PayoutAccount, Dispute; VerificationResult gained `reason` (rejection).
  STATE MACHINE (state.ts): RATE_LOCKED -> PAYOUT_FAILED; PAYOUT_FAILED -> RATE_LOCKED|
    REFUNDED; COMPLETED -> DISPUTED; DISPUTED -> REVERSED|COMPLETED; REVERSED terminal.
    COMPLETED is no longer terminal (dispute window) — state.test.ts updated accordingly.
  PRISMA: PaymentState += 3 values; new enum DisputeStatus; new models PayoutAccount,
    Dispute (+ User/Payment relations). Migration 20260904130000_unhappy_paths
    (ALTER TYPE ADD VALUE + 2 tables). Additive; not applied to any live DB.
  BACKEND:
  - verification: KYC/KYB can now be REJECTED (sanctions hit / short tax-id /
    documentRef contains "fail") with a reason + resubmit; success message now points
    to adding a payout account. (Real onboarding that can fail — FR-1.)
  - payouts module: add/list/deactivate payout accounts + hasActivePayoutAccount().
    confirmPayment gates on it: no active dst-currency account -> PAYOUT_FAILED (not
    funded). retryPayout() re-drives a failed payout once an account exists.
  - disputes module: either party opens a dispute on a COMPLETED payment -> DISPUTED;
    admin resolves REFUND (escrow refund -> REVERSED) or DISMISS (-> COMPLETED).
    Reuses the settlement seam's refund(); notifies both parties + admins.
  - seed.ts: verified demo freelancers get an INR payout account so the happy path
    + real-chain e2e stay green; new self-serve signups add theirs via the UI.
  - app.ts registers payoutAccountRoutes + disputeRoutes; payment.routes adds /retry.
  FRONTEND: api client methods; freelancer Payout methods page (+nav); PaymentDetail
    gains raise-dispute (party, COMPLETED), retry-payout (company, PAYOUT_FAILED), and
    DISPUTED/PAYOUT_FAILED banners; Verify page shows rejection reason; Admin Monitor
    gains a Disputes queue (Refund/Dismiss) + open-disputes stat.
  VERIFIED: shared build clean; backend typecheck + 56 tests green (was 45, +11);
    frontend typecheck + vite production build clean. Migration additive, not yet
    applied to any live DB (prisma migrate deploy at hosting time).

=====================================================================
2026-09-04 — BACKEND GAP CLOSURE (branch claude/backend-repo-analysis-rwvtei)
Ten gaps found by auditing the live backend on main against PRD/TRD/BUILD_CONTRACTS.

SHARED (/shared — interface treaty, changes logged here as §1 requires):
  - enums.ts: ESCROW_MODES / EscrowMode = 'INSTANT' | 'HOLD' (new, additive).
  - schemas.ts: createPaymentSchema += escrowMode (optional, defaults INSTANT).
    CreatePaymentInput is now z.input<> rather than z.infer<> so callers that
    build the input by hand may omit the defaulted field; a parsed request body
    still satisfies it. No existing caller changes.
  - types.ts: Payment += escrowMode; new FreelancerSummary (roster wire shape).

BUILD_CONTRACTS §4 DEVIATION, RESOLVED (not a contract change):
  The contract freezes POST /kyc/submit, POST /kyb/submit and
  POST /admin/verify/:userId. The live backend had grown /verification/kyc,
  /verification/kyb and /verification/me instead, and no admin-verify path at
  all. All three frozen paths are now served by the same handlers; the
  /verification/* paths stay, so neither the contract nor existing consumers
  move. POST /admin/verify/:userId issues the credential + on-chain hash through
  the same provision() the self-serve flow uses.

BACKEND:
  - payments: Actor ({id, role}) on every public entry point, with assertParty
    (read: payer, payee or admin) and assertPayer (write: paying company or
    admin). getPayment() previously took a requesterId and ignored it, and
    confirm/retry/release/refund never checked ownership. Split into checked
    getPayment() and internal getPaymentInternal() for pre-authorized callers.
  - documents: receipt/compliance/firc/descriptor get the same party check.
  - admin module (new): GET /admin/metrics (was mock-only; the frontend has
    always called it), POST /admin/expire-locks.
  - RATE_LOCKED -> EXPIRED now actually happens (timer + endpoint). It was
    declared in the state machine from the start and never produced.
  - metrics.tick broadcast to admins every 5s (BUILD_CONTRACTS §5), skipped when
    no admin is connected.
  - notifications module (new): GET /notifications, /unread-count,
    POST /:id/read, /read-all. The duplicated notify() helpers in verification
    and disputes now call the one shared write path.
  - directory module (new): GET /directory/freelancers — the roster the payout
    wizard needs (it shipped three hardcoded seed UUIDs).
  - createPayment validates the payee (must be a FREELANCER, must be VERIFIED)
    before writing a payment or a compliance decision.
  - escrowMode HOLD stops confirm at FUNDED; release-on-work-approval settles it
    (FR-2.2). Migration 20260904140000_escrow_mode (new enum + defaulted column;
    additive, existing rows read INSTANT).
  - env: METRICS_TICK_SECONDS (5), RATE_LOCK_SWEEP_SECONDS (60).

FRONTEND: api client gains freelancers(), releasePayment(), refundPayment();
  NewPayout loads the live roster and offers the escrow choice; PaymentDetail
  gains the approve-and-release / refund panel for a held escrow.

OPS: backend + frontend Dockerfiles, both wired into infra/docker-compose.yml so
  `docker compose up` boots the whole stack (NFR-4); root eslint + prettier
  config (pnpm lint could not run at all before); GitHub Actions CI running
  typecheck, tests and lint.

[2026-09-04] AI ADJUDICATION + CUSTOMER MGMT + DASHBOARDS v2
  SHARED CONTRACT (logged): added createCustomerSchema + CustomerSummary type
    (additive). No existing values changed. shared rebuilt.
  BACKEND:
  - agent/adjudicator.ts: triages FLAGged payments -> AUTO_CLEAR / AUTO_REJECT /
    ESCALATE, confidence-gated (>=0.75). Safety floor: sanctions/structuring/
    >USD25k always escalate; LLM when ANTHROPIC_API_KEY set else heuristic; errors
    leave FLAGGED. Wired into payment.service createPayment FLAG branch; recorded on
    ComplianceDecision + audit. env AI_ADJUDICATION (default true). 6 unit tests.
  - customers module: GET /customers (admin all / company freelancers), GET
    /customers/:id, POST /customers (company->freelancer, admin->either; verified
    provisions wallet+credential so a new freelancer is payable via
    /directory/freelancers). Registered in app.ts.
  FRONTEND: Customers page (company+admin nav); Operator monitor v2 (metrics row +
    AI-adjudication summary + escalations-only queue); Company Overview v2 (quick
    actions, needs-attention tile, corridor mix). Removed agent-panel left border.
  VERIFIED: shared build; backend typecheck + 109 tests; frontend typecheck + vite
    build — all green. See docs/AI_ADJUDICATION_AND_OPERATIONS.md.

=====================================================================
2026-09-04 — FRONTEND GAP CLOSURE (CORRIDOR_ROADMAP items 2, 3, 4)
Mirror of the backend audit: endpoints that existed with no UI consumer.

SHARED (/shared — logged here as roadmap §1 requires):
  - types.ts: AdjudicationAction, AdjudicationEntry, AdjudicationSummary (AI
    adjudication metrics); CorridorHolding, Treasury (operator treasury view).
    Additive. The backend services import these rather than declaring their own,
    so the wire shape cannot drift between the two sides.

BACKEND:
  - admin/adjudication.service.ts (new): GET /admin/adjudications. No schema
    change — the adjudicator already writes AuditLog PAYMENT_ADJUDICATED_<ACTION>
    and stamps ComplianceDecision.reviewedBy 'ai:*' / reviewNote with the
    confidence and rationale; this reads those rows back and reports how much of
    the queue the agent settles without a human.
  - admin/treasury.service.ts (new): GET /admin/treasury — escrow held per
    corridor (FUNDED/SETTLING/DISPUTED) and fee revenue on COMPLETED, converted
    to USD minor through the same FX source quotes use.

FRONTEND:
  - Notification centre: bell + unread badge + history dropdown + mark read/all,
    and live toasts. /notifications had NO client method at all — anything not
    caught live was lost on reload (FR-7.1).
  - PaymentDetail renders GET /payments/:id/documents instead of hardcoding two
    buttons and re-deriving availability, so FIRC appears and disabled controls
    carry the backend's own reason.
  - /me/identity (new): credential card from /credentials/me — DID, on-chain
    hash, validity, anchor tx, certificate PDF. Previously unconsumed.
  - /me/history (new): per-payment effective rate, fee and share kept.
  - /admin/rules (new) consuming the long-unused api.rules().
  - /admin/treasury (new).
  - EXPIRED: chip styling + a PaymentDetail banner. The backend has swept stale
    rate locks into this state since the last session and the UI rendered it as
    an unstyled chip explained nowhere.

BUGS FIXED IN THE v2 DASHBOARD:
  - Monitor multiplied metrics.flaggedPct by 100, but the backend already returns
    a percentage — 4.2% rendered as 420%.
  - Monitor's avg-settlement stat fell back to a hardcoded 47 when metrics were
    absent; it now shows an em dash until real numbers arrive.
  - Alert details were dumped with JSON.stringify; the message and rule id are
    rendered as fields.
  - Monitor now takes metrics.tick off the websocket instead of re-polling.
  - Freelancer Home showed "You kept 99.25%" as a literal; computed from real fees.

STYLES: the new CSS uses --ink-soft; there is no --muted custom property in this
  design system (only a .muted class), so var(--muted) would have silently
  fallen back to inherited colour.

2026-09-04 (cont.) — WIZARD + COUNTERPARTY NAMES
SHARED: types.ts Payment += companyName, freelancerName (nullable). Both parties
  already see both names on the receipt, so this exposes nothing new; it removes
  a second round trip per table and the UUID-prefix placeholders.
BACKEND: payment.service.ts serialize() + the three queries feeding it now
  include the company/freelancer name relation. Mock fixtures updated.
FRONTEND: NewPayout rebuilt as the 4-step wizard (UI_SPEC 3.2) — stepper, payee
  search, inline "+ New payee" via createCustomer, currency + FEMA purpose-code
  pickers, live quote with the vs-incumbent strip, and a rate-lock countdown ring
  that disables confirm when the lock lapses. Overview / freelancer Home /
  History / PaymentDetail render names instead of UUID prefixes.

2026-09-05 — UPI LEG / INR OFF-RAMP (last mile), phase 1: contract + data model
SHARED: enums.ts += PAYOUT_METHODS ['BANK','UPI'] + PayoutMethod. types.ts
  PayoutAccount += method, vpa; accountName/accountNumberMasked/bankIdentifier are
  now nullable (a UPI method has no bank fields). schemas.ts addPayoutAccountSchema
  gains optional `method` (absent ⇒ BANK, back-compat) + `vpa` (VPA_REGEX), with a
  superRefine requiring the right fields per method. Additive — existing BANK
  callers are unchanged.
BACKEND: prisma PayoutAccount += method (default 'BANK') + vpa, bank columns made
  nullable; Payment += payoutMethod + payoutRailRef (how the INR was delivered
  off-ramp). Migration 20260905060000_upi_leg (committed, not applied).
  payout-account.service.ts create()/serialize() handle BANK vs UPI;
  hasActivePayoutAccount() unchanged — a UPI account is a valid INR destination.

2026-09-05 (cont.) — UPI LEG phase 4: off-ramp drives the CREDITED step
SHARED: types.ts Payment += payoutMethod (PayoutMethod|null) + payoutRailRef
  (string|null) — how the payee was credited after on-chain release. Additive,
  null until COMPLETED.
BACKEND: payment.service.ts creditPayee() resolves the payee's active dst-currency
  account, pushes it through the payout rail (getPayoutRail), and stores
  payoutMethod + payoutRailRef; called on both COMPLETED paths (straight-through
  settle + release-on-approval). serialize() exposes the two fields.
  payout-rail.interface.ts += pure destinationFromAccount(). Tests added.

{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd .. && corepack enable && pnpm install --frozen-lockfile && pnpm --filter ./frontend build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
